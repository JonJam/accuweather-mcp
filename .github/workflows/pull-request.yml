name: Pull request

on:
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  build:
    uses: ./.github/workflows/gradle-build.yml
    with:
      app-version: ${{ github.head_ref }}-SNAPSHOT
    secrets: inherit

